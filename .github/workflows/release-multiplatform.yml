name: Multiplatform Native Installers

on:
  push:
    tags:
      - 'v*'

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-installers:
    name: Build for ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        include:
          - os: ubuntu-latest
            artifact-name: MatchingEngine-Linux.tar.gz
          - os: windows-latest
            artifact-name: MatchingEngine-Windows.zip
          - os: macos-latest
            artifact-name: MatchingEngine-Mac.tar.gz

    steps:
    - uses: actions/checkout@v4

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: maven

    # Corregido: Distingue entre el script de Linux/Mac y el ejecutable .cmd de Windows
    - name: Build native installer (jlink + jpackage)
      shell: bash
      run: |
        if [ "${{ matrix.os }}" = "windows-latest" ]; then
          ./mvnw.cmd clean compile javafx:jlink jpackage:jpackage
        else
          chmod +x mvnw
          ./mvnw clean compile javafx:jlink jpackage:jpackage
        fi

    # Corregido: Buscamos la carpeta generada dinámicamente sin importar dónde la deje jpackage
    - name: Prepare artifact
      shell: bash
      run: |
        # Buscamos de forma recursiva la carpeta "MatchingEngine" dentro de target/ para evitar fallos de rutas fijas
        TARGET_DIR=$(find target -type d -name "MatchingEngine" | head -n 1)
        
        # Si find no funciona en Windows Git-Bash, probamos una ruta alternativa estándar
        if [ -z "$TARGET_DIR" ]; then
          TARGET_DIR="target/installer/MatchingEngine"
        fi
        
        if [ "${{ matrix.os }}" = "windows-latest" ]; then
          # Usamos 7z apuntando al directorio encontrado
          7z a ${{ matrix.artifact-name }} ./${TARGET_DIR}
        else
          tar -czf ${{ matrix.artifact-name }} ./${TARGET_DIR}
        fi

    # Mantenemos tu paso de subida que está perfecto
    - name: Upload to GitHub Release
      uses: softprops/action-gh-release@v2
      with:
        files: ${{ matrix.artifact-name }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
